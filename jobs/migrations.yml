apiVersion: batch/v1
kind: Job
metadata:
  name: migrations
  labels:
    name: migrations
spec:
  completions: 1
  template:
    metadata:
      name: migrations
    spec:
      containers:
        - image: us.gcr.io/ispa-176718/ispa:latest
          name: migrations
          command: [ "/run_migrations.sh ]
      restartPolicy: Never
